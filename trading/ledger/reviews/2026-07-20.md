## 오늘 요약

- **2026-07-20 / 무거래일.** 진입 0, 청산 0, 보유 0종목, 전액 현금 1,000만원. 일간 수익률 0bp.
- 유일한 의사결정은 **시장 폭 24% < 50% → 신규 진입 전면 중단 (C2 필터)**. 보유가 없어 청산 로직은 애초에 실행할 대상이 없었다.
- `brain_ok: true`, 킬스위치·일일손실한도 모두 미발동 (`state/gate_status.json`: `killswitch_engaged=false`, `daily_limit_used_pct=0.0`).
- **로그에서 발견한 이상치 2건** (아래 개선 후보 참조): 프리마켓이 오늘 **두 번** 실행됨(00:58:08, 01:04:10), `equity_log.jsonl`에 **동일 날짜 3중 기록**.

## 판단 품질 (진입/청산/스킵 근거의 타당성)

**스킵 근거 — 타당.** 시장 폭 24%는 임계 50%의 절반 이하다. `autotrader/pipeline.py:198-202`의 폭 계산(유니버스 중 20일 이평 위 비율)에 비추어, 유니버스 4분의 1만 추세 위에 있는 국면이다. 경계선(예: 48%) 판정이었다면 임계값 민감도를 따져볼 여지가 있지만, 24%는 어떤 합리적 임계값(0.3/0.4/0.5 스윕 후보 전부)에서도 동일하게 차단된다. **파라미터 선택이 결과를 갈랐다고 볼 수 없는 명백한 케이스**다.

**진입 판단 — 평가 불가.** 스킵이 후보 스코어링보다 상위에서 걸렸기 때문에, "폭 필터가 없었다면 어떤 종목을 샀을 것인가"가 로그에 남지 않았다. 즉 오늘 필터의 기회비용을 사후에 측정할 수 없다. 이건 판단 오류는 아니지만 **관측 공백**이다.

**청산 판단 — 해당 없음.** 보유 0.

**주의할 점:** 무거래일이 곧 좋은 판단은 아니다. 손실 0은 리스크를 안 졌기 때문이지 예측이 맞았기 때문이 아니다. C2 채택 근거였던 MDD 개선은 정확히 이런 날들의 누적으로 만들어지지만, CLAUDE.md에 기록된 **약세·횡보 국면 샤프 열위(2024 전반 -1.42 vs V0 -0.14)** 관찰 조건이 바로 이 국면이다. 오늘은 그 관찰 대상 표본 1일차로 기록해 두는 게 맞다.

## 리스크 관찰 (한도 대비 사용률)

| 항목 | 현재 | 상태 |
|---|---|---|
| 일일 손실 한도 사용률 | 0.0% | 여유 |
| 킬스위치 | 미발동 | 정상 |
| 포지션 수 / 노출 | 0 / 0% | 전액 현금 |
| 쿨다운 등록 종목 | 없음 (`cooldowns.json` = `{}`) | — |
| 게이트 거절 | 1건 (= 폭 필터) | 의도된 동작 |

**우려점 — 리스크 소진이 아니라 리스크 미가동 쪽이다.**

1. **자본 유휴.** 전액 현금 상태에서 폭 필터가 지속되면 기회비용이 무제한 누적된다. 방어 파라미터가 "영구 관망"으로 굳는 실패 모드를 감시할 필요가 있다.
2. **폭 필터의 재진입 경로 부재.** 현재 구조는 폭이 임계 위로 회복돼야만 진입이 열린다. 회복 시점이 반등의 상당 부분이 지난 뒤일 가능성 — 이건 백테스트가 이미 반영한 비용이므로 지금 고칠 대상은 아니고, **관찰 항목**으로 둔다.
3. **연속 차단 일수를 세는 지표가 없다.** 며칠째 차단인지 로그만으로는 즉시 안 보인다.

## 개선 후보 (다음 실행 전 점검할 것)

우선순위 순. **전부 관찰·계측 항목이며, 전략 파라미터 변경 제안은 없다.**

1. **[버그 의심 · 최우선] 프리마켓 중복 실행.** `premarket_log.jsonl`에 오늘 00:58:08과 01:04:10 두 건이 6분 간격으로 남았다. 오늘은 진입이 0이라 무해했지만, **진입이 열린 날 같은 일이 벌어지면 주문이 2배로 나갈 수 있다.** 스케줄러 트리거와 `run_premarket.py`의 당일 재실행 가드를 확인할 것. (최근 커밋 `171e94463 feat: 트레이딩 일정 자동 실행`과의 연관 여부 확인 필요.)

2. **[데이터 무결성] `equity_log.jsonl` 동일 날짜 3중 기록.** 2026-07-20 항목이 3번 append됐다. 집계·백테스트 비교에서 날짜 중복은 통계를 왜곡한다. 날짜 키 upsert 또는 append 전 중복 체크 필요.

3. **[실행 시각] 프리마켓이 새벽 01시대에 실행됨.** KST 개장 8시간 전이다. t-1 봉 기준 계산이라면 논리적으로는 무해하지만, 의도된 스케줄인지 확인할 것. 의도치 않았다면 1·3번이 같은 원인일 수 있다.

4. **[관측성] 차단 시에도 후보 스코어를 로깅.** 폭 필터로 진입이 막혀도 "필터가 없었다면 진입했을 종목/스코어"를 기록하면, 필터의 기회비용을 사후에 실측할 수 있다. CLAUDE.md의 "약세 국면 성과 관찰" 조건을 데이터로 채우는 가장 저렴한 수단.

5. **[관측성] 연속 차단 일수 + 폭 시계열 기록.** 폭 값 자체(24%)를 별도 필드로 남기고 연속 차단 카운터를 유지하면, 방어 모드가 얼마나 오래 지속되는지 즉시 보인다.

**미검증 고지:** 이번 회고는 로그·상태 파일 판독만 수행했다. 1·2번 항목은 코드 확인 없이 로그 증상만으로 제기한 **의심**이며, 테스트는 실행하지 않았다. 원인 규명이 필요하면 별도로 지시해 달라.
