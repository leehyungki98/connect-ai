# DESKS — 트레이딩 데스크 조직

모의투자 전용 워크스페이스. 각 데스크는 독립 조직이다.

## 데스크 목록

| 데스크 | 폴더 | 시장 | 스타일 | 체결 | LLM |
|---|---|---|---|---|---|
| 스윙팀 | `trading/` | 한국 주식 | 일봉 스윙 (~10거래일) | KIS 모의투자 API | codex 공격 / claude 방어 |
| 미장팀 | `us-longterm/` | 미국 주식 | 코어 장기 (분기 리밸런싱) | 내부 페이퍼 엔진 | 없음 (판단은 코드 밖 절차) |

## 조직

```
사용자 (오너 — 모든 승인권)
│ 지휘 라인: 사용자 → CEO → 총괄팀장      보고 라인: 총괄팀장 → 비서 → 사용자
│
├── CEO 🧭 — 두 데스크 총괄. 업무 분배·우선순위, 자금배분 "제안", 데스크 간 조율,
│           두 총괄팀장 보고 취합 (매매·자금배분 승인권은 사용자)
│   ├── 스윙팀 (trading/) — 한국 주식 스윙
│   │   ├── 임재훈 🎯     총괄팀장 — 일일 파이프라인 운영, 레오·판정자 조율
│   │   ├── 레오 📈       선정자 (codex, 공격)
│   │   ├── 판정자 ⚖️      기각 기준 적용 (claude, 방어)
│   │   └── 현빈 📊       사후분석
│   └── 미장팀 (us-longterm/) — 미국 주식 장기
│       ├── 한도윤 🏛️     총괄팀장 — 분기 리뷰 오케스트레이션, 실행 순서 관리
│       ├── 서지호 🔭     선정자 — 편입 후보 발굴, 3기준 예비 판정
│       ├── 민서율 🧮     판정자 — 숫자로만 판정, 스토리 불인정
│       ├── 강해원 🌏     정세분석가 — 뉴스·지정학 수집·수치화 (매매 발동 권한 없음)
│       ├── 노유진 📐     사후분석 — NAV vs SPY, 환율 분해 + 정세 접붙임 (매일 07:10)
│       └── 윤가온 🧩     정리 담당 — 전문가 근거를 질문에 맞게 엮어 쉬운 말로 (온디맨드)
└── 공용
    ├── 코다리 💻        코드 담당 — 승인된 제안만 구현 (편집 금지 구역 제외)
    └── 영숙 📱          비서 — 각 총괄팀장에게 보고받아 사용자 전달 (텔레그램)
```

보고 라인: 실무 → 각 팀 총괄팀장(임재훈/한도윤) → CEO / 비서(영숙) → 사용자.

정의 위치: `src/agents.ts` (AGENTS 맵의 `desk` 필드 + DESK_TEAMS).
초상화는 `assets/agents/{이름}.jpeg` — 파일이 없으면 이모지로 폴백한다.

### 권한 경계 (위반 불가)

1. **체결 경로에 에이전트 금지.** 미장팀 매매(분기 리밸런싱)는 결정적 코드가 전부.
   선정자·판정자의 무대는 분기 리뷰(편입/퇴출 **제안**)이지 주문이 아니다.
2. **CEO 전결 화이트리스트** (2026-07-20 사용자 승인): ① 읽기 전용 실행(리포트
   재생성·데이터 재검토 지시) ② 조사 태스크 배분(결과는 제안서로만) ③ 주문 없는
   정기 스크립트 실행(run_daily 등). "소규모인가"를 CEO가 판단하지 않는다 —
   이 목록에 있는 것만 전결, 결과는 비서 경유 사후 보고.
   코드·설정·전략 변경, 리밸런싱 실행, 자금배분 변경은 **항상 사용자 승인**.
3. **정세분석가 정보 출구 3개 제한**: ① 분기 리뷰의 질적 리스크 서술
   ② 사후분석 해석 ③ 긴급 보고 → **사용자의** 킬스위치 판단.
   금지 출구: 리밸런싱 타이밍·비중·주문에 뉴스/정세를 반영하는 모든 경로 —
   허용되는 순간 타이밍 전략으로 변질된다.
4. **에이전트 산출물의 종착지는 승인 카드.** 승인 전 코드·구성 어디에도 반영 안 됨.
5. **자동화 도입 게이트**: 스케줄·자동 소집·비서 자동 보고는 미장팀 DoD 통과 +
   분기 사이클 1회 무사고 완주 + ledger 근거 기록 후, 항목별 개별 사용자 승인으로만.
   자동화 대상은 "실행·보고"뿐 — 승인 지점은 어떤 경우에도 자동화하지 않는다.

## 자금 배분 (모의 — 이 표가 단일 출처)

**총 자본 1,000만원** (2026-07-20 사용자 확정 — 실전 규모와 동일하게 맞춘다.
모의를 실전보다 크게 굴리면 수수료·최소단위 영향이 과소평가되어 검증이 전이되지 않는다.)

| 데스크 | 비율 | 자본 | 반영 상태 |
|---|---|---|---|
| 스윙팀 | 25% | 250만원 | ✅ `SWING_ALLOCATION_KRW` (pipeline 이 계좌 잔고를 지분으로 축소) |
| 미장팀 | 75% | 750만원 | ✅ `us-longterm/longcore/config.py` |

**두 데스크는 지갑을 공유하지 않는다.** 각자 자기 배분액으로 사이징하며,
한쪽이 다른 쪽 잔액을 끌어다 쓰는 경로는 존재하지 않는다.

### 스윙 자본이 config 숫자만으로 안 되는 이유

`config.INITIAL_CAPITAL_KRW` 는 **백테스트 기준액일 뿐**이다. 실매매 수량은
KIS 모의계좌의 **실제 잔고**(1,000만원)를 보고 계산한다. 계좌 금액은 줄일 수단이
없으므로 `pipeline.desk_portfolio()` 가 잔고를 지분으로 축소한다:

```
데스크 지분 = 250만원 + (현재 계좌 평가액 − 1,000만원)
```

**고정 상한이 아니라 offset 인 이유:** 번 돈이 재투자돼야 복리가 산다. 이 계좌는
스윙 외 매매가 없으므로 계좌 손익 전부가 스윙 손익이다. 벌면 지분이 늘고 잃으면 준다.

**부작용 (2026-07-22 확인):** 종목당 한도가 375,000원이 되면서 **고가주가 사실상
배제된다** — 삼성바이오로직스(139만원/주)는 1주도 못 산다. 종목당 2~5주라 1주 차이가
포지션의 20~50%다. 사용자가 이를 알고 25/75 배분을 택했다.

**섀도 계좌 층은 1,000만원을 유지한다** (사용자 선택). 따라서 섀도는 실계좌가 못 사는
종목도 산다 — 섀도 성적을 실계좌 성적으로 그대로 환산하면 안 된다. 폭 구간별 통계를
내는 샘플 층은 개별 트레이드 기준이라 영향 없다.

### 분기 점검일 알림 (2026-07-22 신설)

분기 점검일 = **1·4·7·10월 첫 거래일**. D-7 부터 매일 카운트다운한다
(07:00 스냅샷 출력 + 대시보드 카드 배너, D-2 부터 빨강).

리밸런싱은 자동이 아니라 **사람이 여는 절차**다. 그래서 실제 실패 모드는
'규칙이 틀리는 것'이 아니라 **'날짜를 몰라서 그냥 지나가는 것'**이다.

⚠ 카운트다운(`longcore/schedule.py`)은 **표시 전용 추정**이다. 미래 날짜라 시세
인덱스가 없어 달력으로 근사한다(주말·신정·성금요일만 반영). 체결을 허가하는 권한은
`gates/rebalance_gate.py` 뿐이며 그쪽은 실제 거래일로 판정한다.
**D-0 이라고 게이트가 열리는 게 아니고, 게이트가 닫혀 있다고 알림이 틀린 것도 아니다.**

### 미결 — 데스크간 분기 재배분

총자본을 분기마다 25/75 로 다시 맞추는 층은 **설계도 코드도 없다**(2026-07-22 확인).
지금은 각 데스크가 자기 돈만 굴리고, 미장팀 리밸런싱은 자기 버킷 안(ETF/성장주/현금)
에서만 돈다. 한쪽이 크게 벌면 비율이 25/75 에서 벗어난 채로 계속 간다.
도입하려면 별도 설계·승인이 필요하다.

변경은 사용자 승인 후 이 표에서 먼저 수정하고 각 데스크에 반영한다.

## 상호 규칙

1. 데스크 간 코드 공유·import·복붙 금지. 패턴(킬스위치·게이트·state/ledger 구분)은
   철학으로만 공유하고 구현은 각자 소유한다.
2. 서로의 폴더를 수정하지 않는다 (읽기만 허용).
3. 킬스위치·안전층·state 는 데스크별 독립 — 한쪽 차단이 다른 쪽에 전파되지 않는다.
4. 공통 헌법: 안전층은 100% 결정적 코드(LLM 금지), 실거래 연결 금지,
   "완료/검증됨" 보고 전 실제 테스트 출력 첨부, 전략 변경은 검증 + 사용자 승인 후에만.
5. 새 데스크 추가 시: 이 표에 등록 → 자금 배분 재승인 → 별도 폴더 신설.
